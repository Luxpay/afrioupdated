import 'package:flutter/material.dart';

class PaymentMethodObj {
  int id;
  String title;
  String subTitle;
  String img;
  Color colour;

  PaymentMethodObj(this.id, this.title, this.subTitle, this.img, this.colour);
}
